
from django.urls import path
from . import views

urlpatterns = [
    # Add your health tracking URLs here when you implement them
    # path('vitals/', views.vitals_view, name='vitals'),
    # path('activities/', views.activities_view, name='activities'),
]
